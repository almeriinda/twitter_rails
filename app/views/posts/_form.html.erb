<%= form_with(model: post, :html => { :multipart => true }) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<div class="card">
  <div class="authorFooter">
   <% image_user = current_user.image.present? ? current_user.image : '/assets/img/user2-160x160.png' %>
   <%= image_tag image_user, style: 'height:73px;width:73px;' %>
   <div class="authorInfo">
      <div class="row">
         <p class="authorName"><%= form.text_field :title, placeholder: "O que está acontecendo?", class: "form-control" %></p>
         <p class="authorLink"><%= form.file_field :image, placeholder: "Anexe uma imagem", class: "form-control" %></p>
         <p class="authorBody"><%= form.text_area :information, placeholder: "Descreva...", class: "form-control" %></p>
         <%= form.hidden_field :user_id, value: current_user.id %>
      </div> 
   </div>
  </div> 
    <div class="button_right1">
    <%= form.submit "Tweetar", class: "btn btn-primary" %>
  </div>
  </div>  

<% end %>

<style>
  .button_right1 {
    padding-left: 870px;
    padding-bottom: 20px;
  }
</style>
