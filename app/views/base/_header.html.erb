<!-- Navbar -->
<nav class="main-header navbar navbar-expand navbar-white navbar-light">
    <!-- Left navbar links -->
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i class="fas fa-bars"></i></a>
        </li>
    </ul>

    <!-- Right navbar links -->
    <ul class="navbar-nav ml-auto">
        <!-- Messages Dropdown Menu -->
        
        <!-- Notifications Dropdown Menu -->
        <li class="nav-item dropdown">
            <a class="nav-link" data-toggle="dropdown" href="#" data-placement="top" data-toggle="tooltip" href="#" data-original-title="Tooltip on top">
                <i class="far fa-bell"></i>
                <span class="badge badge-warning navbar-badge"><%= get_notifications_user(current_user) %></span>
            </a>
        </li>
         <li class="nav-item">
          <%= link_to destroy_user_session_path(:authenticity_token => form_authenticity_token()), class:"nav-link dropdown-item", method: :delete do %>
            <i class="fas fa-sign-out-alt"></i> Sair
          <% end %>
        </li>

    </ul>
</nav>
<!-- /.navbar -->
